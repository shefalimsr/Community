<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pay1.community">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:label="Training Material"
            android:name=".TrainingMaterial.FeedActivity" >

        </activity>

        <activity
            android:label="Blog"
            android:name=".blog.BlogActivity" >

        </activity>

        <activity
            android:label="Company Update"
            android:name=".CompUpdt.UpdateActivity" >

        </activity>

        <activity
            android:label="PAY1 Community"
            android:name=".home.HomeActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


    </application>


</manifest>